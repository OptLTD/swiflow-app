/** Default agent key; pairs with DEFAULT_PROVIDER_NAME via agent.provider. */
export const DEFAULT_AGENT_KEY = 'default'

/** Default provider name; agent.provider references this. */
export const DEFAULT_PROVIDER_NAME = 'default'
