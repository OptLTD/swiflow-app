/** Default agent key; pairs with DEFAULT_PROVIDER_NAME via agent.txt_model. */
export const DEFAULT_AGENT_KEY = 'default'

/** Default text provider name; agent.txt_model references this. */
export const DEFAULT_PROVIDER_NAME = 'default'

/** Default vision provider name; agent.img_model references this. */
export const DEFAULT_VISION_PROVIDER_NAME = 'vision'
